apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-lapak
spec:
  acme:
    email: imam@blast.co.id
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: letsencrypt-lapak
    solvers:
    - http01:
        ingress:
          class: nginx